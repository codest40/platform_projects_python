CPU(
    usage_percent=24.1,
    logical_cores=8,
    physical_cores=4,
    load_average=(0.61,0.55,0.48),
    frequency_mhz=3294,
)

How busy is the CPU?

How many cores?

Load average?

CPU frequency?

CPU model?

Per-core utilization?
